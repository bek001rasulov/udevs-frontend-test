import {NavbarWrapper} from "./style.ts";

const Navbar = () => {
    return (
        <NavbarWrapper>
            <p>Cегодняшние заказы</p>
        </NavbarWrapper>
    );
};

export default Navbar;
